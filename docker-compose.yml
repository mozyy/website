version: "3.6"
services:

  nginx:
    build:
      context: .
      dockerfile: nginx.Dockerfile
    ports:
      - "80:80"
      - "443:443"

  go:
    build:
      context: .
      dockerfile: go.Dockerfile
    ports:
      - "6503:6503"
